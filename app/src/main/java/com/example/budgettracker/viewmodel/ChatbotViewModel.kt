package com.example.budgettracker.viewmodel

import androidx.lifecycle.ViewModel

/**
 * ViewModel for the Chatbot.
 * Manages conversation history and processes user queries via the ChatbotEngine.
 */
class ChatbotViewModel : ViewModel() {
    // Reserved for chatbot interaction state
}
