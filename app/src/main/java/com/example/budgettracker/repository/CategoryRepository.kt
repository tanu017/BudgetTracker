package com.example.budgettracker.repository

/**
 * Repository for Category data.
 * Manages the classification of transactions (e.g., Food, Travel).
 */
class CategoryRepository {
    // Reserved for category data operations
}
