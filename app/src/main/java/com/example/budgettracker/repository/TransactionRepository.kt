package com.example.budgettracker.repository

/**
 * Repository for Transaction data.
 * Acts as the mediator between the Transaction DAO and the ViewModel.
 */
class TransactionRepository {
    // Reserved for transaction data operations
}
